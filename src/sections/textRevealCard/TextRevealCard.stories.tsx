import React from "react";
import TextRevealCard from "./TextRevealCard";

export default {
  title: "Sections/TextRevealCard",
  Component: TextRevealCard,
};

export const Component = (args: any) => <TextRevealCard {...args} />;
